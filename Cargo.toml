[package]
name = "fathom"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.5", features = ["http2"]}
handlebars = "4.0"
once_cell = "1.10.0"
tokio = { version = "1", features = ["full"]}
tracing = "0.1"
tracing-subscriber = "0.2"
